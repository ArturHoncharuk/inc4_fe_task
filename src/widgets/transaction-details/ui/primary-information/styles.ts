import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    marginTop: 16,
  },
  hash: {
    marginTop: 8,
    fontSize: 16,
    width: '100%',
    color: 'white',
  },
  statusKey: {
    fontSize: 16,
  },
  details: {
    marginTop: 8,
    flexDirection: 'row',
    alignItems: 'center',
  },
});
